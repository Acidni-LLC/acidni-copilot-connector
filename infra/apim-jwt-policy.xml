<!--
  APIM JWT Validation Policy for Terprint Copilot Connector
  
  Deploy to: apim-terprint-dev.azure-api.net
  Apply to: All Operations (inbound policy)
  
  This policy validates OAuth 2.0 JWT tokens from Microsoft Copilot and extracts
  user context for personalization features (favorites, recommendations).
-->
<policies>
    <inbound>
        <base />
        
        <!-- Validate JWT from Microsoft Copilot / Teams -->
        <validate-jwt header-name="Authorization" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized. Access token is missing or invalid.">
            <openid-config url="https://login.microsoftonline.com/{{AAD_TENANT_ID}}/v2.0/.well-known/openid-configuration" />
            <audiences>
                <audience>api://{{AAD_APP_CLIENT_ID}}</audience>
            </audiences>
            <issuers>
                <issuer>https://login.microsoftonline.com/{{AAD_TENANT_ID}}/v2.0</issuer>
                <issuer>https://sts.windows.net/{{AAD_TENANT_ID}}/</issuer>
            </issuers>
            <required-claims>
                <claim name="scp" match="any">
                    <value>Terprint.Access</value>
                </claim>
            </required-claims>
        </validate-jwt>
        
        <!-- Extract user identity from validated token -->
        <set-variable name="userId" value="@(context.Request.Headers.GetValueOrDefault("Authorization","").Split(' ').Last().AsJwt()?.Claims.GetValueOrDefault("oid", "anonymous"))" />
        <set-variable name="userEmail" value="@(context.Request.Headers.GetValueOrDefault("Authorization","").Split(' ').Last().AsJwt()?.Claims.GetValueOrDefault("email", context.Request.Headers.GetValueOrDefault("Authorization","").Split(' ').Last().AsJwt()?.Claims.GetValueOrDefault("upn", "")))" />
        <set-variable name="userName" value="@(context.Request.Headers.GetValueOrDefault("Authorization","").Split(' ').Last().AsJwt()?.Claims.GetValueOrDefault("name", ""))" />
        
        <!-- Forward user context to backend -->
        <set-header name="X-User-Id" exists-action="override">
            <value>@((string)context.Variables["userId"])</value>
        </set-header>
        <set-header name="X-User-Email" exists-action="override">
            <value>@((string)context.Variables["userEmail"])</value>
        </set-header>
        <set-header name="X-User-Name" exists-action="override">
            <value>@((string)context.Variables["userName"])</value>
        </set-header>
        
        <!-- Add correlation ID for tracing -->
        <set-header name="X-Correlation-Id" exists-action="skip">
            <value>@(Guid.NewGuid().ToString())</value>
        </set-header>
        
        <!-- Rate limiting per user -->
        <rate-limit-by-key calls="100" renewal-period="60" counter-key="@((string)context.Variables["userId"])" />
        
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
        <!-- Add security headers -->
        <set-header name="X-Content-Type-Options" exists-action="override">
            <value>nosniff</value>
        </set-header>
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
